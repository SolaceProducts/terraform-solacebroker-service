version: 3

project:
  locator: SolaceProducts_terraform-solacebroker-service
  id: SolaceProducts_terraform-solacebroker-service
  name: terraform-solacebroker-service
  teams: []
  labels:
    - terraform

vendoredDependencies:
  forceRescans: false
  scanMethod: CLILicenseScan
  licenseScanPathFilters:
    exclude:
      - "./.git"
      - "./.github"

paths:
  exclude:
    - ./.git
    - ./.github

telemetry:
  scope: full
